 /*
 constants 
 types of constants---Integer const(1,2,3,o,-1,-2), Real constants(1.0,3.14,-2.4), Character constants('a','b','#','&')

 Keywords- reserved words that have special meaning to the compiler.
 total are 32 words and these are as follows:
 auto,double,int,struct,
 break,else,long,switch,
 case,enum,register,typedef,
 char,extern,return,union,
 continue,for,signed,void,
 do,if,static,while,
 default,goto,sizeof,volatile,
 const,float,short,unsigned.
*/
